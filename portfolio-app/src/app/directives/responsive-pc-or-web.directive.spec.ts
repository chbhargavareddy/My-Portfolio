import { ResponsivePcOrWebDirective } from './responsive-pc-or-web.directive';

describe('ResponsivePcOrWebDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsivePcOrWebDirective();
    expect(directive).toBeTruthy();
  });
});
